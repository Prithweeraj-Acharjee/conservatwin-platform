services:
  - type: web
    name: conservatwin-platform-api
    plan: free
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: CT_DB_PATH
        value: /tmp/conservatwin.db
